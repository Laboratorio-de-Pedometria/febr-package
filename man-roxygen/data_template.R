#' @param variable Name(s) of the variable(s). If missing, then a set of standard identification variables is
#' downloaded. Use \code{variable = "all"} to download all variables. See \sQuote{Details} for more information.
#' 
#' @param stack Should tables from different datasets be stacked on a single table for output? Defaults to
#' \code{stack = FALSE}, the output being a list of tables.
#' 
